name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: althu123696
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push FastAPI image
        run: |
          IMAGE_ID="ghcr.io/${GITHUB_REPOSITORY,,}/fastapi:latest"
          echo "Building and pushing $IMAGE_ID"
          docker buildx build --push -t "$IMAGE_ID" -f app/Dockerfile .

      - name: Build and push Streamlit image
        run: |
          IMAGE_ID="ghcr.io/${GITHUB_REPOSITORY,,}/streamlit:latest"
          echo "Building and pushing $IMAGE_ID"
          docker buildx build --push -t "$IMAGE_ID" -f streamlit/Dockerfile .

  deploy:
    runs-on: self-hosted
    needs: build
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: althu123696
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Deploy with docker-compose
        run: |
          docker compose -f docker-compose.yml pull
          docker compose -f docker-compose.yml up -d
